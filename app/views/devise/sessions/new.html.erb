

<div class="grid place-items-center h-screen ">
<div class="bg-[url('background.svg')] h-screen w-screen  bg-no-repeat bg-cover  absolute "></div>
  <div class="bg-gray-100 dark:bg-gray-900 rounded-lg p-10 max-w-sm space-y-2" data-aos="fade" data-dutaion='1000' >


    <%= link_to site_root_path, class: "btn-secondary-gray relative -top-4 -left-6 " do %>
      <%= icon('fa-solid','arrow-left', class:'-mx-1')%>
    <% end %>


    <div class="space-y-10 mb-6">
      <%= image_tag('favicon.svg', class:"w-full h-48 animate-breathe") %>
      <%= image_tag('favtext-dark.svg', class:"w-full block dark:hidden") %>
      <%= image_tag('favtext-white.svg', class:"w-full dark:block hidden") %>
    </div>

    <div class="flex justify-center space-x-2 pt-4">
        <%= link_to App::Admin.model_name.human, :new_admin_session, class:"p-2 text-xs #{resource_name.to_s=='admin' ? 'bg-blue-600 dark:bg-blue-600 text-white '  : ' bg-gray-200 dark:bg-gray-700 '}  dark:hover:bg-blue-700 rounded-lg hover:bg-blue-700 hover:text-white" %>
        <%= link_to App::User.model_name.human, :new_user_session, class:"p-2 text-xs #{resource_name.to_s=='user' ? 'bg-blue-600 dark:bg-blue-600 text-white' : 'bg-gray-200 dark:bg-gray-700'}    dark:hover:bg-blue-700 rounded-lg hover:bg-blue-700 hover:text-white" %>
    </div>

    <%= form_for(resource, as: resource_name, url: session_path(resource_name), html: {class: "space-y-2", data:{turbo:false} })  do |f| %>

      <div class="">
        <%= f.label :email %>
        <%= f.email_field :email, class:'form-input bg-gray-200 dark:bg-gray-200 peer-default:bg-red-600 dark:text-gray-900', autofocus: true, autocomplete: "email" %>
      </div>

      <div class="">
        <%= f.label :password %>
        <%= f.password_field :password, class:'form-input bg-gray-200 dark:bg-gray-200 dark:text-gray-900', autocomplete: "current-password" %>
      </div>

      <div class="pt-2 w-full flex justify-between items-center flex-row-reverse ">
        <%= f.submit t('devise.sessions.new.sign_in'), class:'btn-primary ' %>

        <% if devise_mapping.rememberable? %>
          <div class="flex flex-row-reverse items-center text-sm">
            <%= f.label :remember_me, class:'ml-2 ' %>
            <%= f.check_box :remember_me, class:'form-checkbox-simple bg-gray-200 dark:bg-gray-200' %>
          </div>
        <% end %>
      </div>

    <% end %>
    <div class="text-xs">
    <%= render "devise/shared/links" %>
    </div>

  </div>
</div>
